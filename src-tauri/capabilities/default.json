{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "sidebar"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "core:window:allow-set-size"
  ]
}
